# TREK 增强路线（尚未接入）

本项目没有可用 TREK 实例、OAuth 连接、服务器或公网域名。当前只交付本地攻略，禁止把下面的接入计划当完成证据。

上游：[liketrek/TREK](https://github.com/liketrek/TREK)、[MCP 文档入口](https://github.com/liketrek/TREK/wiki/MCP-Overview)。2026-09-22 检查 README/MCP.md；MCP.md 目前指向 wiki，具体工具和权限必须在实际版本发现，不能照搬第三方微信版 Key 或接口。

维护者统一部署，上游实现与 Skill 分开维护。部署前锁定实际 release/commit，读取对应官方 Compose、环境变量和许可；不猜镜像标签、默认密码和 API。配置持久化、HTTPS、备份恢复，使用两个普通测试用户。不要为了这个增强另建第二份 CloudBase 行程数据。

在已授权测试环境中以“一天三个虚构地点”验证：创建、回读、修改、再次回读、第二用户不能访问私有行程、只读分享不能修改。手机蜂窝网络打开、电脑关闭仍可访问，需要真实设备验收，不由桌面自动化代替。

同步适配的前置交付是版本化映射表：本地日期/地点/顺序/预算/拍照建议/备选/来源分别对应实际对象、字段、工具和前端展示位置。短备注可以做连通性验证，不能当独立卡片完成。未找到扩展字段时先说明缺口，不静默丢弃内容。只接受受控主题，不上传任意 HTML/JS。

更新前回读版本和内容，再计算变更；远端有新修改时合并或让用户决定。写后回读核对每个字段与归属。请求超时先查询是否已提交，禁止直接重放创建。连接凭证留在用户授权环境，不进入 trip.json、HTML、日志和 Skill。

按用户当前授权判断发布/分享范围，首次连接只走实际官方授权流程。没有共享授权时保持私有；授权已明确时不要重复询问。

排障预算：具备环境后单次验证默认最多 45 分钟；缺少服务器或授权即记录阻碍，达到预算记录失败层与已做检查，保留本地交付，不无限排障。用户可明确调整预算。上线前复核上游许可与修改分发要求，不以“公开仓库”代替授权判断。
